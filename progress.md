Original prompt: git hub pagesに公開し、そこからそれぞれ選んだキャラで遊べるバーチャル空間を作りたい！
今、assetsフォルダにそれぞれのプレイヤーを作ってるんだけど、どちらかというと上から見た視点でスマホ用なのでバーチャルコントローラで動かせて、idleとrunができるように
まだできてない画像もあるのでそれは空白として、front、back、side（左右反転）を使います
それぞれ入った人とで位置などが同期されるようにしたいし、それぞれキャラはまず初期起動時のキャラ選択で選べるようにするんですが、それで選んだ後はもうこれを選んでいる人がいますという感じで、それを選べなくなるようにしましょう
イメージとしては画面に全部のplayerのidleアニメーションとともに名前が
ラフト、まい、やんさん、たぬつな、ムート、もろん、ウィーク、ギョーザとしましょう

- 初期化: 既存ファイルを確認。web用の実装ファイルは未作成。
- 方針: GitHub Pages対応の静的構成で、Yjs + y-webrtcで位置同期/キャラ占有を実現する。
- 実装: index.html/style.css/app.js を新規作成。
- 追加機能: 8キャラ選択UI、Yjs+y-webrtc同期、キャラロック、仮想スティック移動、idle/run描画、画像未作成時プレースホルダー。
- 追加フック: window.render_game_to_text / window.advanceTime(ms)
- 検証: Playwrightクライアントで選択->移動を実行。state JSONで座標更新を確認。
- 検証: スクリーンショットを確認し、キャラ描画/名前表示/見下ろしグリッド背景を確認。
- 検証: errors JSON が出力されないことを確認（404とCORSエラー解消済み）。
- TODO: 実機2台で同一部屋参加し、キャラロック/位置同期の体感テストを行う。
- UI改修: ゲーム画面を常時全画面表示に変更。キャラ選択はゲーム上のオーバーレイ表示に変更。
- UI改修: input/buttonをappearance:noneでカスタム化し、ブラウザ標準UI見た目を排除。
- 再検証: 選択->移動のPlaywright実行成功（state-0.json確認）。
- 調整: ワールドサイズを 4800x3600 に拡張。
- 調整: 未作成フレームは透明スプライトを描画する仕様に変更。
- 調整: 実アセット更新（mai/raft）に合わせて利用可能ファイル定義を更新し、404回避。
- 検証: Playwright再実行でerrorsファイル未生成（コンソールエラーなし）。
- 追加: 各キャラフォルダに front/back/side x idle/run x 1/2 の不足PNGを透明画像で自動生成（既存ファイルは非上書き）。
- 改修: スプライト解決を side_ 命名対応へ変更し、固定ファイル一覧依存を削除。
- 検証: 選択画面ロード時の404が消え、Playwright実行でerrorsファイル未生成を確認。
- 調整: スプライト探索は front/back/side 系のみ参照へ統一（不要な idle/run 直参照を削除）。
- 調整: 選択画面プレビューを front 方向優先へ変更。
- 検証: Playwright 2シナリオ実行で404なしを確認。
- 修正: 透明補完フレームを判定し、実画像を優先して選ぶロジックを追加。
- 修正: side方向で run/idle が透明の場合、front/back の実フレームへフォールバック。
- 検証: mai選択で移動シナリオ実行、404なしを確認。
- 改修: スプライト読み込みを固定参照ロジックへ変更（透明判定/存在判定フォールバックを削除）。
- 改修: 横移動は front_* を使う固定ルールに変更し、mai/front_run の適用を保証。
- 検証: mai選択 + 横移動で描画確認、404なし。
- 修正: 画像URLに ?v=20260307-2 を付与し、ブラウザ/GitHub Pagesキャッシュの古い透明画像を回避。
- 検証: サーバーログで version付きアセット取得を確認。
- 仕様統一: 全プレイヤー共通で front/back/side × idle1/2, run1/2 の固定命名参照に統一。
- 仕様統一: 横移動は side_* を必ず使用。
- 仕様統一: 走りアニメは run1/run2 の2フレーム交互。
- 検証: tanutsuna で移動シナリオ実行、404なしを確認。
- 修正: アセットのキャッシュバスターを固定値から実行時timestampへ変更。
- 修正: 画像ロード失敗をnull固定せず、次フレームで再試行可能に変更。
- 検証: version付きURLで取得、404なしを確認。
- 改修: スプライト参照を全件明示マッピングへ変更（8キャラ x 3方向 x idle/run x 1/2）。
- 改修: 動的パス組み立てをやめ、定義済みパスのみロードする方式へ変更。
- 改修: 起動時に全スプライトを事前ロードするよう追加。
- 検証: Playwright実行で404なしを確認。
- UI刷新: HTMLの標準input/button UIを撤去し、キャラ選択・部屋表示・戻る/コピー操作をCanvas描画UIへ移行。
- UI操作: Canvas上の pointer/click/touch で選択・HUDボタン操作を処理。
- 調整: スマホ画面でも8キャラが収まるよう選択カードを自動スケーリング。
- 検証: PlaywrightでCanvas UI描画とエラー無しを確認。
- 修正: `wss://demos.yjs.dev` が 403 のため y-websocket を撤去し、y-webrtc の複数シグナリングサーバー指定へ変更。
- 修正: 部屋IDを `a-z0-9_-` の小文字に正規化し、端末間で room hash が揺れても同一ルームへ入るよう統一。
- 修正: SYNC表示をRTC接続状態基準に変更し、WebSocket失敗で常時CONNECTINGになる問題を解消。
- 修正: BitTorrentトラッカー経由の検出不安定対策として Trystero 戦略を `torrent` から `nostr` へ変更。
- 修正: Nostr relay を複数指定（damus/nos.lol/nostr.band/offchain）し、端末環境差での接続失敗を軽減。
- 修正: 同期戦略を `trystero/firebase` に変更し、Firebaseプロジェクト (`raft-studio-virtual`) を利用する構成へ移行。
- 設定: 受領した firebaseConfig を `app.js` に反映（databaseURL は `https://raft-studio-virtual-default-rtdb.firebaseio.com` を使用）。
- 修正: `Service database is not available` 対策として trystero 依存を外し、Firebase Realtime Database SDK 直接同期へ移行。
- 修正: `players` と `locks` を `rooms/{roomId}/...` に保存、onValue購読 + onDisconnect削除で入退室同期する構成に変更。
- 調整: 同期描画を補間方式に変更（remote player の位置を低遅延スムージング表示）。
- 調整: ネット送信頻度を間引き（約80ms間隔）し、受信揺れによるカクつきを低減。
- 調整: remote 補間を「受信点追従」から「短時間の速度予測付き補間」に変更し、減速→急ジャンプ見えを低減。
- 調整: 補間係数を引き上げ（`REMOTE_SMOOTH_RATE=14`）て追従感を改善。
- 調整: アニメ速度を減速（run: 10fps相当 -> 6fps相当、idle: 5fps相当 -> 3fps相当）。
- 確認: `side` 方向も `run1,idle1,run2,idle1` の同一シーケンスで再生される実装を維持。
- やんさん農業統合(進行中): `ソース/やんさん農業/game.js` を多人数版へ接続。
- 修正: 透明化の主因だったアセット解決を修正（`assets/player/*` はリポジトリ直下 `assets/`、農業アセットは `ソース/やんさん農業/assets/` を参照）。
- 修正: `char_select` の pointer 入力を実装（COPY URL / カード選択で PLAY）。
- 修正: Firebase同期を起動時に必ず開始するよう変更（`setupMultiplayerSync()` 呼び出し追加）。
- 修正: キャラロックを `runTransaction` で競合防止。失敗時はローカル参加フォールバックを追加。
- 修正: ロックheartbeat更新を `syncLocalPlayer` に追加。
- 修正: `index.html` をやんさん農業版起動に切替（`tile.js`,`terrain.js`,`game.js(module)`）。
- 修正: `ソース/やんさん農業/index.html` も `game.js` を module で読み込むよう修正。
- 検証: develop-web-game Playwright client 実行。
- 検証結果: `output/web-game-yansan-3/state-0.json` で `mode:"play"` 遷移と座標更新を確認。
- 検証結果: `output/web-game-yansan-3/shot-0.png` で選択後プレイ画面・ドット描画を確認。
- 残課題: 同室2端末の同時接続(PEERS増加/他プレイヤー可視化)は実機2台で最終確認が必要。
- 2026-03-08: `ソース/やんさん農業/index2.html` をプレイヤー別ツール編集対応。
- 追加: プレイヤー選択UI（RAFT/MAI/TANUTSUNA/YANSAN/MUTO/MORON/WEEK/GYOZA）。
- 変更: ベース参照を `../../assets/player/<player>/<frame>.png` に切替（見つからない場合 `assets/yansan/<frame>.png` へフォールバック）。
- 変更: ツール保存先を `assets/skin/<item>/<player>/<frame>.png` に変更（旧 `assets/skin/<item>/<frame>.png` も読込フォールバック）。
- 変更: スキン下書きキャッシュキーを `player::skin` 化し、プレイヤーごとに独立管理。
- 変更: save API payload に `playerName` を追加。
- 検証メモ: `index2` は通常HTTPだと `/api/bootstrap` がない環境で404になるため、file:// またはAPI同梱環境で確認する。
- 2026-03-08: `index2` のフレーム定義を idle2 までに統一（idle3系を削除）。
- 2026-03-08: `index2` の初期アイテムをツール参照へ変更（`sickle/schop/hoes/soil_sifter/hammer`）。
- 2026-03-08: `index2` のプレイヤー選択を追加し、ベース画像を `../../assets/player/<player>/<frame>.png` 参照へ変更。
- 2026-03-08: `index2` のツール保存先を `assets/skin/<item>/<player>/<frame>.png` に変更（旧パス読込フォールバックあり）。
- 2026-03-08: ゲーム本体にスマホUI追加（十字キー、耕す、インベントリ、1-9スロット選択）。
- 2026-03-08: スマホUIはプレイ中のみ表示、全画面要求＋横向きロックを試行、スロット選択状態をUI反映。
- 2026-03-08: root/index.html と ソース/やんさん農業/index.html の両方へモバイル操作UIレイヤーを追加。
- 2026-03-08: develop-web-game手順で再検証 (`output/web-game-mobile-final`) を実施。
- 検証結果: `state-0.json` で `mode:"play"`、座標・インベントリ・ツール配置が正常、`errors-0.json` 未生成（新規コンソールエラーなし）。
- 2026-03-08: 十字キーを `assets/ui/up|down|left|right.png` 参照へ変更（root/index と ソース/index の両方）。
- 2026-03-08: キャラ選択UIを `assets/ui/player_select.png` + `assets/ui/kyara_select.png` 優先描画へ戻し。
- 2026-03-08: 選択可否インジケータを固定で 緑(可)/赤(不可) の四角表示に修正。
- 2026-03-08: Firebase同期エラーの握りつぶしを停止し、`SYNC ERROR` 表示に変更（onValue cancel / write失敗を表示）。
- 2026-03-08: Playwright検証 `output/web-game-ui-fix/shot-0.png` でUI反映確認。
- 2026-03-08: side向きが全員逆だったため、左右反転の判定を `left` 基準から `right` 基準に統一。
- 対象: `ソース/やんさん農業/game.js`（本体/遠隔プレイヤー/衝突マスク/ツール重ね）と `app.js`（root版描画）。
- 2026-03-08: プレイ中のクイックスロットを直接クリック/タップで選択できるように変更（canvas HUD hit-test追加）。
- 2026-03-08: クイックスロットHUDを画面サイズ依存で自動レイアウト化（狭い画面は横並び/縮小、広い画面は従来縦並び）。
- 2026-03-08: キャラ参加をオンライン接続必須にし、ロック取得はFirebase transaction成功時のみ許可（フォールバック書き込みを削除）。
- 2026-03-08: 長押しで出るブラウザメニュー抑止を強化（contextmenu/dragstart/selectstart抑止、touch-callout無効）。
- 2026-03-08: モバイル操作ボタン文言を「耕す」から「使う」に変更。
- 2026-03-08: プレイ中は全画面維持を強化（fullscreenchangeで再要求）。
- 2026-03-08: キャラ描画をローカル/リモート統合ソート描画へ変更。Y座標で全員を同一順序描画し、奥/手前の前後関係を正しく反映。
- 2026-03-08: リモートプレイヤー補間（指数平滑）を追加し、カクつきを低減。
- 2026-03-08: ライトシェーダの重い合成を無効化（`SHADER.enableCinematic=false`）して軽量化。
- 2026-03-08: タイル変更を Firebase `rooms/<room>/world/tileOverrides` へ同期する処理を追加（購読+送信）。
- 2026-03-08: インベントリ画面を完全レスポンシブ化し、常に画面内へ収まるよう再計算。閉じるボタンを追加。
- 2026-03-08: モバイルUIを inventory 中も表示し、閉じ操作不能を解消。
- 2026-03-08: キャラ選択/セーブスロット画面を可変カラム化し、小画面で溢れないレイアウトへ変更。
- 2026-03-08: モバイルUI調整: 十字キーを小型化して上へ移動、アクション/スロット帯も上方向に再配置。
- 2026-03-08: モバイルスロット表記を1-9番号から空スロットUIへ変更（スロット自体をタップ選択）。
- 2026-03-08: インベントリ中はモバイル操作UIを非表示に変更し、操作重なりを解消。
- 2026-03-08: キャラ選択画面をさらに小型化し、縦スライド（ドラッグ）対応を追加。
- 2026-03-08: キャラ選択画面にスクロール量インジケータを追加。
- 2026-03-08: Playwright検証 `output/web-game-ui-slide-fix/shot-0.png` で全8キャラが画面内表示されることを確認。
- 2026-03-08: ゴースト表示対策としてプレイヤー同期データにTTLを追加（`PLAYER_TIMEOUT_MS` 超過エントリを除外）。
- 2026-03-08: モバイルの補助クイックバーを非表示化し、スロットはゲーム内スロット描画のみを使用する構成へ統一。
- 2026-03-08: キャラ選択を「常時8人表示（4x2固定）」へ変更。スクロール無しで全員見えるように調整。
- 2026-03-08: 選択カード・文字・PLAYボタンを小画面向けにさらに縮小。
- 2026-03-08: Playwright確認 `output/web-game-char-all-visible/shot-0.png` で全キャラ同時表示を確認。
- 2026-03-08: モバイル操作UIをCSS配置依存からCanvas(JS描画)主体へ移行開始（十字キー/使う/インベントリを`drawTabButton`で描画）。
- 2026-03-08: 縦持ち時でもプレイ中は`requestFullscreen + orientation.lock(landscape)`を継続試行するよう強化。
- 2026-03-08: クイックスロット位置をJS側の描画位置で上方へ移動（compact時）。
- 2026-03-08: Playwright検証 `output/web-game-js-ui-only/shot-0.png` で表示崩れなしを確認。
- 2026-03-08: スマホデフォルトズームをアウト側へ変更（`defaultZoom=0.95`）し、畑侵入時の自動ズーム変更ロジックを停止。
- 2026-03-08: Canvas(JS)描画UIにズームIN/OUTボタン（使うボタン上の上下）を追加。最小/最大は `CAMERA.minZoom` / `CAMERA.maxZoom` で制限。
- 2026-03-08: プレイ中の横向き固定を継続試行し、縦持ち開始でも横画面化を強化。
- 2026-03-08: クイックスロット帯をJS描画位置でさらに上へ移動。
- 2026-03-08: Playwright検証 `output/web-game-zoom-ui` 実施、エラーなし。
- 2026-03-08: キャラ別セーブを追加（`yansan_farm_character_state_v1`）。キャラ離脱時保存、次回同キャラ選択時に復元。
- 2026-03-08: オートセーブ間隔を5分へ変更（`AUTOSAVE_INTERVAL=300`）。
- 2026-03-08: 5分オートセーブ時に既存スロット保存に加えてキャラ別保存も実行。
- 2026-03-08: `F9` で全キャラ退出（`rooms/<room>/players` と `locks` を削除）できる機能を追加。
- 2026-03-08: スマホ左右移動フリーズ対策として、Touch HUD入力クリア処理 `clearTouchHudInput()` を追加。
- 2026-03-08: `pointercancel` / `pointerleave` / `window blur` / `visibilitychange` で入力を確実に解除し、押しっぱなし残留を防止。
- 2026-03-08: Touch HUD押下時に `pointerId` を保持し、対応する入力だけを解放するよう修正。
- 2026-03-08: 毎フレームの `requestGameplayFullscreen()` 呼び出しを停止し、入力時のみ要求する方式へ変更（負荷軽減）。
- 2026-03-08: Canvas HUDで十字キー画像を `assets/ui/up|down|left|right.png` 描画する実装を維持確認。
- 2026-03-08: Playwright検証 `output/web-game-touch-freeze-fix-play` 実施。`state-0.json` で `mode:"play"` と移動反映を確認、スクリーンショットでゲーム描画確認、errors生成なし。
- 2026-03-08: インベントリ表示中にクイックスロット帯が重ならないよう、下部予約領域(`quickBarReserve`)を導入してバッググリッドを上寄せ。
- 2026-03-08: クイックスロット帯はインベントリ中も最下部に描画し、ドロップ判定(`quickRects`)を維持。
- 2026-03-08: Touch HUDを複数ポインタ対応に変更（`touchHudPointers`）。移動入力と「使う」押下の同時保持に対応。
- 2026-03-08: HUD入力解除をポインタ単位で再計算する方式に変更し、片方を離してももう片方の入力が維持されるよう修正。
- 2026-03-08: キャラ別保持強化として `collectedToolKinds` を追加（sickle/schop/hoes/soil_sifter/hammer）。
- 2026-03-08: セーブデータに `characterProgress.collectedTools` を保存し、ロード時に優先復元。
- 2026-03-08: ロード時に取得済みツールを `worldItems` に再適用し、未取得ツールのみ画面に表示されるよう修正。
- 2026-03-08: `ensureRequiredToolsAvailable` は取得済みツールを再スポーンしないよう変更。
- 2026-03-08: ツール取得直後に `saveCharacterState(true)` を実行し、モバイル終了時でも所持品が消えにくいよう改善。
- 2026-03-08: Playwright検証 `output/web-game-character-tools-save-fix/state-0.json` で `sickle` の `collected:true` とインベントリ所持を確認。
- 2026-03-08: クイックスロットが低すぎる問題に対応。`drawInventoryUI` の下端マージンを調整し、スマホプレイ時はスロット帯をより上へ移動。
- 2026-03-08: インベントリ表示時は被り回避を維持したまま、スロット帯の位置を少し上げる調整。
- 2026-03-08: Playwright確認 `output/web-game-slot-y-adjust/shot-0.png` でプレイ中スロット位置が上がったことを確認。
- 2026-03-08: `使う`ボタンで収穫できるよう変更。手持ちツールがない場合は `tryHarvestClambonAtForwardTile()` を実行する仕様へ。
- 2026-03-08: タイル同期を全量`set`から差分`update`へ変更（`pendingTilePatches`導入）。
- 2026-03-08: リモート受信時は未送信ローカル差分を上書きしないよう、snapshotにローカルpending差分を再適用してから反映。
- 2026-03-08: 送信成功後は送信済み差分のみpendingから除去し、他端末の同時編集時でも未送信ローカル変更が消えないよう修正。
- 2026-03-08: Playwright確認 `output/web-game-use-harvest-tile-sync-fix` 実施（起動・操作継続OK）。
- 2026-03-08: キャラ状態保存を端末localStorage依存からFirebase優先へ変更。
- 2026-03-08: 進行データ保存先を `characterStates/<characterId>` に追加し、デバイスを跨いでも同一キャラの所持/進行を共有する構成に変更。
- 2026-03-08: キャラ参加時はFirebaseの状態を先にロードし、存在しない場合のみローカルキャッシュへフォールバック。
- 2026-03-08: キャラ参加時に保存データが無い場合は `resetGameplayState()` で初期化し、別キャラ状態の持ち越しを防止。
- 2026-03-08: Playwright確認 `output/web-game-global-character-save` 実施（起動/プレイ遷移OK）。
